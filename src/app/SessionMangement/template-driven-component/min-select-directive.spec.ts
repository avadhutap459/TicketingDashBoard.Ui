import { MinSelectDirective } from './min-select-directive';

describe('MinSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new MinSelectDirective();
    expect(directive).toBeTruthy();
  });
});
